type celebsData = {
  first: string;
  last: string;
  dob: string;
  picture: string;
  description: string;
  country: string;
  gender: string;
};
export type CelebAccordionProps = {
  celebsData: celebsData[];
};
